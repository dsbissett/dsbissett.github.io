export type Rgb = readonly [number, number, number];

/** Cool porcelain white for tank, bowl exterior, seat and lid. */
export const PORCELAIN: Rgb = [0.93, 0.93, 0.965];

/** Slightly shadowed porcelain used just inside the bowl rim. */
export const PORCELAIN_SHADOW: Rgb = [0.7, 0.72, 0.8];

/** Deep interior tone at the bottom of the bowl for readable depth. */
export const BOWL_DARK: Rgb = [0.28, 0.31, 0.4];

/** Chrome tone for the flush handle. */
export const CHROME: Rgb = [0.74, 0.77, 0.85];

/** Background clear color (matches the site's deep-space base). */
export const CLEAR_COLOR: Rgb = [0.02, 0.035, 0.08];
